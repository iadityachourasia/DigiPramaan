Use `CategoryTile` for a grid of browsable categories or regions.

```jsx
<CategoryTile icon="label_off" label="Labelling" count={64} />
```

- Show `count` so users can see where the volume is before clicking.
