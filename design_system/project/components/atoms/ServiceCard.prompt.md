Use `ServiceCard` on service dashboards and landing pages.

```jsx
<ServiceCard icon="qr_code_scanner" title="Scan a product"
  description="Record a field scan and check label compliance" meta="Average 2 minutes" />
```

- One link per card. Do not nest buttons inside it.
- Say how long the service takes in `meta` when you know — it is the question users ask.
