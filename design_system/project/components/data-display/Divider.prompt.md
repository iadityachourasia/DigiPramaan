Use `Divider` only where whitespace alone fails to separate.

```jsx
<Divider spacing="l" />
<Divider label="Officer remarks" />
<Divider orientation="vertical" />
```

- `dashed` marks an optional or draft boundary, matching the source's Dash axis.
