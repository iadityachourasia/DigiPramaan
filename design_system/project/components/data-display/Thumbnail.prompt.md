Use `Thumbnail` in the thumbnail-plus-name cell pattern.

```jsx
<Thumbnail src="/scans/a2201.jpg" alt="Scan of batch A-2201" size="S" />
```

- `alt` describes the scan; decorative thumbnails pass `alt=""`.
- Never stretch — the ratio is fixed and the image covers.
