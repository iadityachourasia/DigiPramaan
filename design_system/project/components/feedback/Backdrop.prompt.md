Use `Backdrop` when you need the scrim without Modal or Drawer.

```jsx
<Backdrop strength="strong"><Spinner size="L" label="Uploading scan…" /></Backdrop>
```

- `strength="strong"` for destructive confirmations; keep `blur` off unless the design calls for it.
