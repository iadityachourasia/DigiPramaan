Use `Popover` for explanation or small controls anchored to a trigger.

```jsx
<Popover title="How the compliance rate is calculated" placement="bottom">
  Compliant scans divided by total scans in the selected period.
</Popover>
```

- Dismissible and non-blocking. Text-only hints belong in `Tooltip`.
