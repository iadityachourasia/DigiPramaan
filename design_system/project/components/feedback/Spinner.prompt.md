Use `Spinner` for short, unmeasurable waits.

```jsx
<Spinner size="S" label="Verifying Aadhaar" showLabel />
```

- `tone="light"` on dark or brand-filled surfaces.
- Always keep the label, visible or screen-reader-only; a bare ring announces nothing.
