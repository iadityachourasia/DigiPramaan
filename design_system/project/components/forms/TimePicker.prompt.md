Use `TimePicker` for appointment and slot times.

```jsx
<TimePicker label="Slot start" value="10:30 AM" />
```

- Minutes step in 15s by default — match the real booking granularity.
- `use24Hour` drops the period column.
