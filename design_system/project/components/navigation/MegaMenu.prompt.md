Use `MegaMenu` when a nav item leads to more than about seven destinations.

```jsx
<MegaMenu columns={[{title:'Enforcement', links:[{label:'Inspections', icon:'fact_check', description:'Record a field inspection'}]}]} />
```

- Three to four columns, group titles always present.
- Descriptions are optional but help when destination names are ambiguous.
